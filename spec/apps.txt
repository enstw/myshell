# Desired applications
# The agent resolves the best install method per platform.
# Optional notes after # help the agent pick the right package.
# Prefix: [mac] macOS only, [ubuntu] Ubuntu only, [optional] skip unless explicitly requested

# Core tools
git
zsh
jq
tmux
fzf
ripgrep
p7zip
imagemagick
ffmpeg
uv                      # Python package/venv/tool manager — replaces pip, pipx, venv, pyenv
yt-dlp                  # YouTube downloader — install via `uv tool install`
gemini-cli              # AI CLI agent — local install; see constraints.md

# Modern CLI replacements
bat                     # cat replacement
eza                     # modern ls
fd                      # modern find
zoxide                  # smart cd
starship                # cross-shell prompt
tealdeer                # tldr client — must be the Rust tealdeer, not the Python tldr
glow                    # terminal markdown renderer
qrencode

# GUI / desktop
docker                  # Docker Desktop on macOS, docker-ce on Ubuntu
zed                     # code editor

# Academic writing
[optional] quarto        # Quarto publishing system
[optional] pandoc        # universal document converter (pair with xelatex; see constraints.md)

# Platform-specific
[mac] watch
[mac] [optional] proxmark3
[ubuntu] build-essential
