# Archived TODO items (started 2026-06-12; newest dated entries first)

Completed work and resolved incidents moved out of `TODO.md` so it only
tracks live state. Current behavior is documented by the source of truth —
the `scripts/install` header banner and `README.md` — so treat anything
here as history, not as a description of how the scripts work today.
`git log` carries the same history with full diffs.

## Done in stage 2

1. Core apps via brew (mac) or apt + upstream installers for uv/starship/eza/glow (Ubuntu).
1. pnpm installed standalone (`get.pnpm.io` curl|sh, both OSes); pnpm provisions the Node LTS runtime via `pnpm runtime set node lts -g` (migrated off deprecated `pnpm env`). No separate node/npm install — this machine is pnpm-only.
1. `batcat`/`fdfind` → `bat`/`fd` symlinks on Ubuntu.
1. zinit cloned to `~/.local/share/zinit/zinit.git`.
1. Writes `~/.zshenv`, `~/.zsh/{aliases,history,zoxide}.zsh`, `~/.zsh/motd.zsh` (Ubuntu), `~/.zshrc`.
1. Copies `scripts/myshell-update` → `~/bin/myshell-update` (aliased to `u`).
1. Fetches `starship.toml` and eza tokyonight `theme.yml`.
1. Prompts for `git user.name` / `user.email`.
1. Locale generation on Ubuntu: `en_US.UTF-8`, `en_GB.UTF-8`, `zh_TW.UTF-8`; default `LANG=zh_TW.UTF-8`, `LANGUAGE=zh_TW:en`.
1. Timezone set to `Asia/Taipei` (systemsetup on mac; timedatectl or `/etc/timezone` fallback on Ubuntu).
1. pnpm global home is `$PNPM_HOME` (`~/.local/share/pnpm`, no-sudo installs); PATH pickup handled in `~/.zshenv`. `npx` is aliased to `pnpm dlx` and a guard function blocks stray `npm`.
1. AI agents prompt — ONE menu for all four: `0` none, `1` Claude Code, `2` Codex, `3` OpenCode, `4` agy, `5` all; comma-separated numbers or names (`claude,opencode`) work; default `1,2`. The normalized answer is recorded in `~/.local/state/myshell/agents`, so re-runs honor it (including `none`) instead of re-asking — delete the file to re-choose. All installs fail soft. (Gemini CLI dropped — replaced by the non-node-managed `agy`.)
1. Codex — the only pnpm-managed agent (`pnpm add -g @openai/codex` into `$PNPM_HOME`): a static binary with no self-updater, so `pnpm update -g` in `myshell-update` is its update path. npm registry reachability is preflighted inside its installer.
1. Claude Code — native `claude.ai/install.sh` installer into `~/.local/bin/claude` (self-updating; `claude update` in `myshell-update`). NOT pnpm-managed: its auto-updater would fight pnpm, and `$PNPM_HOME` precedes `~/.local/bin` in PATH, so a leftover pnpm copy from older bootstraps would shadow the native binary — the installer migrates it out (`pnpm remove -g`) first.
1. OpenCode — standalone binary via `curl opencode.ai/install | bash -s -- --no-modify-path` into `~/.opencode/bin` (no npm/node, no sudo); PATH pickup in `~/.zshenv`; self-updates via `opencode upgrade` in `myshell-update`. Kept off pnpm to avoid the self-updater-vs-package-manager conflict.
1. Antigravity CLI (`agy`) — the replacement for the retired Gemini CLI. Google's standalone Go binary via `curl antigravity.google/cli/install.sh | bash` into `~/.local/bin/agy` (no npm/node, no sudo; `~/.local/bin` already on PATH); self-updates via `agy update` in `myshell-update`. Standalone like OpenCode, not a pnpm agent.
1. tealdeer config written (`auto_update = true`) and page cache fetched with `LANGUAGE=zh_TW:en`.
1. Opt-in ENS font install from `github.com/enstw/font` — the latest-release asset via the stable `/releases/latest/download/ENSFont.ttc` redirect (verified live 2026-06-10); the answer is recorded so a "no" sticks.
1. Adds zsh to `/etc/shells`, offers `chsh`.
1. Root + no-sudo containers: both stages now route privileged commands through a `$SUDO` shim that is empty when `EUID==0`, `sudo` otherwise, and fails fast with a clear message if non-root and sudo is missing.
1. 2026-07-04 architecture pass (five items) — (1) macOS CI leg: `roundtrip-macos` runs `scripts/ci-roundtrip` directly on `macos-latest` (no ci-roundtrip changes needed — `STATE_DIR`/`PNPM_HOME` are deliberately OS-unified); green on its FIRST run (28700816193), including the first live exercise of the purge paths (removed `brew:pipx`, `brew:node@24` from the runner image); promoted to a blocking gate the same day. Preparing it surfaced and fixed a real bug: `sudo_work_pending` ignored the recorded login-shell answer and the already-zsh case, so a converged Mac (brew zsh is never in `/etc/shells`) would have pre-warmed sudo on every re-run — exactly what the converged-rerun CI assertion forbids. (2) Headless mode settled: answer-store seeding documented as the supported interface (README "Headless / unattended" + the install header's answers rule; `scripts/ci-roundtrip` is the reference implementation). (3) Pairing rule added to the step contract: a tool with its own update path gets its `myshell-update` section in the same commit. (4) Optional-apps growth pattern recorded in TODO (one recorded `choose_` menu + per-app `install_` functions + `apt_keyring_repo`; no data-driven package table). (5) `scripts/check --fix` syncs the output-helper copies from `bootstrap` (canonical because POSIX sh is valid bash).
1. 2026-07-04 action-dedup + conventions pass — `install_eza_ubuntu`/`install_glow_ubuntu` folded into a parametrized `apt_keyring_repo NAME KEY_URL REPO_SPEC PKG` helper (same keyring/list filenames, so converged machines are untouched; `gpg --dearmor --yes` fixes the wedge where an interrupted run left the keyring and the re-run died on gpg's no-tty overwrite prompt); the starship installer gained curl `-f` (an HTTP error page no longer pipes into `sh` as a garbage syntax error); step grammar (`install_`/`configure_`/`deploy_`/`purge_`/`choose_`), per-step semantic guards, and the three OS-branch tiers written into the install header + AGENTS.md; the python/node purge pair documented as deliberately un-shared (the natural extraction would run `warn` in a subshell and silently drop its WARNINGS increment). A full unit×action framework was assessed and rejected: it reverts toward the abandoned `.archive/setup.sh` data-driven model and erases `main()`'s step-level ordering knowledge.
1. 2026-07-04 first green CI (run 28699567420) — after fixing every warning the zero-warning gate had tripped on since 2026-06-14: starship config renamed upstream (now fetches `starship-tokyo-night.toml`, install-once), sudoers drop-in skipped as root, tealdeer < 1.7's dead update URL worked around by seeding the page cache directly, one retry for the pnpm installer download, and `SHELL` exported to that installer (its rc step dies with `ERR_PNPM_UNKNOWN_SHELL` in no-login-env root containers). Both containers pass fresh + idempotent re-run + the headless no-answer abort. The same logs settled the 2026-06-12 unknowns: Node LTS via pnpm 11 works (root job reaches "Node LTS active (v24.18.0)") and `yes | unminimize` completes cleanly in the minimized container. Docker also landed 2026-07-04: Ubuntu `docker.io` + docker group; macOS colima + docker CLI formulae (`colima start` boots the daemon; untested until the bare-Mac leg).
1. 2026-06-14 policy-enforcement pass — the Python-via-uv-only and pnpm-only rules in the header banner are now actively enforced, not just documented. New `purge_conflicts` step (right after `install_apps`) calls `purge_non_uv_python` (strips brew/apt `pipx`/`poetry`/`pyenv` plus `~/.pyenv`, `~/.local/pipx`, `~/.local/share/pypoetry`, `~/.cache/pypoetry`, `~/.local/bin/poetry`; system `python3` untouched) and `purge_non_pnpm_node` (strips brew/apt `node`/`node@20|22|24|lts`/`npm`/`yarn`/`fnm`/`volta`, plus `~/.nvm`/`~/.fnm`/`~/.volta` and just the asdf nodejs plugin; lingering npm-globals in `/usr/local/lib/node_modules`, `/opt/homebrew/lib/node_modules`, `~/.npm-global` get a `warn` instead of being deleted in case they're load-bearing). Both helpers are check-first (clean machines print `Python: nothing to purge (uv-only)` / `Node: nothing to purge (pnpm-only)`) and fail-soft (brew dependency conflicts `warn` instead of tripping the ERR trap). Header rules updated with one-liner pointers to each function so the source-of-truth comments stay next to the code. Verified with `bash -n`; container round-trip with a polluted box pending.
1. 2026-06-12 architecture pass — CI added (`.github/workflows/ci.yml`): `scripts/check` (syntax + byte-identical output-helper enforcement) plus two container round-trips in minimized `ubuntu:24.04` (sudo user and root), each running bootstrap twice headless via `scripts/ci-roundtrip`'s seeded answers and asserting zero warnings. New `unminimize_ubuntu` step: minimized images strip man pages/docs; myshell targets interactive machines, so stage 2 restores them when `/etc/dpkg/dpkg.cfg.d/excludes` marks the system minimized (check-first: unminimize removes the marker). The single-file architecture and the step contract (check-first, gather-recorded prompts, fail-hard-vs-warn, cover in ci-roundtrip) are now written down in the install header.
1. 2026-06-12 fix — `choose_fonts`/`choose_login_shell` no longer record an inferred "yes" when the state already matches (font on disk / already zsh): matching state just skips without writing, so a later deliberate change (deleting the font, switching shells) gets *asked* again on the next run instead of silently reverted — the state files record decisions, not inferred state. `set_login_shell` now matches zsh by basename, not exact path, so `/bin/zsh` vs the brew zsh no longer churns the passwd entry.
1. 2026-06-12 output vocabulary pass — `log`/`sublog`/`warn`/`die` unified across `bootstrap`, `scripts/install`, and `scripts/myshell-update` as byte-identical, POSIX-sh-safe copies (a sourced lib can't reach all three: bootstrap runs pre-checkout, myshell-update is deployed standalone). `warn` goes to stderr and is counted: fail-soft failures and no-sudo deferrals now print `warning:` instead of blending into indented sublog noise, and both long-running scripts end with `Done with N warning(s)` so an unattended run is auditable via `grep '^warning:'`. `myshell-update` gained per-section `log` headers (blank-line separated, needed between brew/apt spew), per-command `|| warn` in place of silent `|| true`, and the same ERR-trap diagnostic as stage 2. Message grammar normalized: in-progress lines end in `...`, results/skips read `<thing> — skipping` with no trailing period.
1. 2026-06-10 review pass — prompts gathered up front (`gather_answers`): git identity, agents, fonts, login shell are all asked in the first seconds and recorded under `~/.local/state/myshell/` (fonts/chsh declines now stick, same as agents), then the run is unattended; sudo is pre-warmed with a keepalive loop so the password prompt can't pop mid-run after a long package phase; `myshell-update` fixes — the zinit block was dead code (`command -v zinit` is always false in bash; now keyed on `zinit.zsh` existing), every section fails soft (`|| true`), and a `$SUDO`/no-sudo shim matches bootstrap; bootstrap — macOS with no sudo and no Homebrew now fails fast instead of dying inside the brew installer, INT/TERM traps exit explicitly (130/143), Debian warns (stderr) that packages are only validated on Ubuntu, and `MYSHELL_APT_UPDATED` lets standalone stage-2 runs refresh a stale apt index exactly once; generated dotfiles now say "do not hand-edit" and `~/.zshrc` documents the `~/.zsh/<name>.zsh` extension point; docs unstaled (`exec` claim, `ent.tw/font`, failure policy now written down in the header).
1. 2026-06-10 agent-install consistency pass — the self-updater rule is now applied uniformly (Claude Code moved from pnpm to the native installer; Codex is the only pnpm agent); all four agents share one menu, so `0 none` truly skips everything; the menu answer is recorded in `~/.local/state/myshell/agents` (the decision, not the installed binaries, is the idempotency key — a `claude`-only or `none` choice no longer re-prompts every run); stale header/README references to the removed `pnpm env` flow corrected.
1. 2026-06-10 idempotency pass — every stage-2 step is now check-first on re-run: brew/apt bulk installs filter to missing packages only; `configure_git` skips when identity is set; `install_agents` skips probe+menu when both agents are in `pnpm ls -g`; `configure_tealdeer` skips `tldr --update` when the page cache exists; terminal profile checks the current default before `open`; container timezone branch compares `/etc/timezone` first; `set_login_shell` reads the passwd entry (`dscl`/`getent`) instead of stale `$SHELL`. New `fetch_config` helper: `starship.toml` / eza theme still refresh each run, but a fetch failure keeps the existing copy instead of tripping the ERR trap (an offline re-run previously died halfway). Verified with `bash -n` + isolated function tests; shellcheck unavailable on this box; full container round-trip still pending.

## Resolved incidents

1. 2026-05-10 fresh Mac bootstrap hit `UNABLE_TO_GET_ISSUER_CERT_LOCALLY` during agent installs, and another run appeared to stop around the Bun installer without the final `Done` line. Stage 2 now forces Homebrew CA postinstall, makes Bun and the pnpm agent installs fail soft, and prints an `ERR` trap diagnostic for unexpected exits.
