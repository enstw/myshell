# Package list
# Format: package_name  or  brew_name:apt_name
# Prefix [mac] for macOS only, [linux] for Ubuntu Linux only

# Common
git
zsh
jq
tmux
qrencode

# macOS only
[mac] watch

# Ubuntu Linux only
[linux] build-essential
