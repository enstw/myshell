# Package list
# Format: package_name  or  brew_name:apt_name
# Prefix [mac] for macOS only, [ubuntu] for Ubuntu only

# Common
git
gh
zsh
jq
tmux
qrencode
glow

# macOS only
[mac] watch

# Ubuntu Linux only (no brew equivalent)
[ubuntu] build-essential

# Cross-platform (brew_name:apt_name where names differ)
npm
bat:batcat
eza
fd:fd-find
fzf
ripgrep
tealdeer:tldr
zoxide
starship
# Docker on macOS: installed as a cask (see packages_cask.txt)
# Docker on Ubuntu: installed via official docker-ce repo (see README Docker section)
p7zip:p7zip-full
imagemagick
ffmpeg
[mac] proxmark3
